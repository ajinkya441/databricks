name: dev deployment

on:
  push:
    branches:
      - main

jobs:
  deploy_to_dev:
    name: "Deploy Bundle (DEV)"
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv

      - uses: databricks/setup-cli@main
      
      - working-directory: .
        env:
          DATABRICKS_CLIENT_ID: ${{ secrets.DATABRICKS_CLIENT_ID_WS_DEV }}
          DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_CLIENT_SECRET_WS_DEV }}
          DATABRICKS_BUNDLE_ENV: dev
        run: |
            databricks bundle validate -t dev
            databricks bundle deploy -t dev
          
      # - name: Bundle deploy (jobs)
      #   env:
      #     DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      #     DATABRICKS_CLIENT_ID: ${{ secrets.DATABRICKS_CLIENT_ID_WS_DEV }}
      #     DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_CLIENT_SECRET }}
      #     DATABRICKS_AUTH_TYPE: oauth-m2m
      #     DATABRICKS_OAUTH_SCOPE: all-apis
      #     BUNDLE_ENV: dev
      #   run: |
      #     set -euo pipefail
      #     if [ -z "${WHEEL_LIBRARY_PATH:-}" ]; then
      #       echo "WHEEL_LIBRARY_PATH is missing; upload step should have set it"
      #       exit 2
      #     fi
      #     if [ -z "${BUNDLE_ENV:-}" ]; then
      #       echo "BUNDLE_ENV is missing; upload step should have set it"
      #       exit 2
      #     fi
      #     # Use the bundle's `ci` target because GitHub runners don't have a local CLI profile.
      #     databricks bundle validate -t ci \
      #       --var="ENV=${BUNDLE_ENV}" \
      #       --var="WHEEL_LIBRARY_PATH=${WHEEL_LIBRARY_PATH}" \
      #       --var="service_principal_id=${{ secrets.DATABRICKS_CLIENT_ID }}"

      #     databricks bundle deploy -t ci \
      #       --var="ENV=${BUNDLE_ENV}" \
      #       --var="WHEEL_LIBRARY_PATH=${WHEEL_LIBRARY_PATH}" \
      #       --var="service_principal_id=${{ secrets.DATABRICKS_CLIENT_ID }}"
