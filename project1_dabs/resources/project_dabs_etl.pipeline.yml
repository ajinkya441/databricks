# The main pipeline for project_dabs

resources:
  pipelines:
    project_dabs_etl:
      name: project_dabs_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/project_dabs_etl"

      libraries:
        - glob:
            include: ../src/project_dabs_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
